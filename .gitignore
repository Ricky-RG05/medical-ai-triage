#Files to be ignored by the git repository!! NEVER TO SHARE gdrive_credentials.json

# Virtual environment
.venv-uv/

# Google credentials — NEVER commit this
gdrive_credentials.json

# Downloaded PDFs — too large, regenerated by gpc_updater.py
PDFs/

# Unnecessary files — not needed for the project
Unnötig/

# Generated reports
reportes_pdfs/

# Python cache
__pycache__/
*.pyc
*.pyo

#chroma_db/
chroma_db/

# Whisper/TTS temp files
*.wav
*.tmp

#MeloTTS has its own .git folder (from which we extracted everything), but we don't need its history (it's frozen in time)
melo_folder/

# VS Code
.vscode/

# Ollama model files
*.onnx
*.bin